architecture: replicaset
replicaCount: 2

persistence:
  storageClass: standard

securityContext:
  runAsUser: 1000
  runAsGroup: 3000
  fsGroup: 2000
  fsGroupChangePolicy: "OnRootMismatch"

volumePermissions:
  enabled: true
